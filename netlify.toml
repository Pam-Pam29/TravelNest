[build]
  command = "cd frontend && npm install && npm run build"
  publish = "frontend/build"

[build.environment]
  NODE_VERSION = "16" # Try a version compatible with your react-scripts
  NPM_FLAGS = "--legacy-peer-deps"
